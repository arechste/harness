# Nested independent clones (per R0-Q6)
repos/*/

# Ephemeral working files
.tmp/
.tmp/persist/

# Claude Code's project-scope state directory.
# .claude/settings.json IS committed (shared project permissions — the harness
# operating regime). Everything else under .claude/ is per-machine:
#   - agents/ and commands/ are symlinks into adapters/claude/ created by
#     bootstrap/setup-host.sh
#   - settings.local.json, sessions, history, etc. are session state
.claude/*
!.claude/settings.json

# OS noise
.DS_Store

# Credential metadata inventory is SECRET-FREE and must be tracked, overriding
# the global *credentials* ignore (~/.gitignore_global). Re-include the dir and
# the inventory; keep any ACTUAL secret material (sops/age/keys) ignored.
!state/credentials/
!state/credentials/INVENTORY.yaml
state/credentials/*.sops.yaml
state/credentials/*.key
state/credentials/*.age
state/credentials/age-key*
